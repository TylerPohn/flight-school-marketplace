# Flight School Marketplace - Root Environment Variables

# AWS Credentials for CDK deployment and DynamoDB operations
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_DEFAULT_REGION=your_aws_region

# CDK Deployment Account (optional - CDK will use default AWS profile if not set)
CDK_DEFAULT_ACCOUNT=your_aws_account_id

# DynamoDB Table Name (used by migration scripts)
TABLE_NAME=your_table_name
